<!DOCTYPE html>
<html>
  <head>
    <title>Switch</title>
    <meta property="og:title" content="Switch-tv" />
    <meta property='og:image' content="<%= image_url('live-chat-final.gif') %>"/>
    <meta property="og:description"
      content="Switch-tv is a Twitch clone, where users can upload their favorite video clips, and chat with others!"
    />
    <meta property="og:url"
      content="https://switch-tv.herokuapp.com/#/"
    />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= favicon_link_tag asset_path('Twitch-logo-reverse.png') %>
    <% if current_user %>
      <script>
        window.currentUser = {
          "id": <%= current_user.id %>,
          "username": "<%= current_user.username %>",
          "email": "<%= current_user.email %>",
          "channel_photo": "<%= url_for(current_user.channel_photo) %>"
        };
      </script>
    <% end %>
    <script>
      window.images = {
        logo: "<%= asset_path("Twitch-logo-reverse.png") %>",
        userPic: "<%= asset_path("generic-user-logo.png") %>",
        ghlogo: "<%= image_url("GitHub_Logo.png") %>",
        lilogo: "<%= image_url("Linkedin-Logo.png") %>",
        allogo: "<%= image_url("AngelList_Black.png") %>",
        anthony: "<%= image_url("ant-b") %>"
      }
    </script>
    <script src="https://kit.fontawesome.com/1576428ae6.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
